# AGENTS.md

## 工作约定

### 需求澄清：先征询是否使用 grill-with-docs

当用户提出新需求 / 新功能 / 较大改动，需要做需求澄清（讨论方案、追问细节、对齐术语）时，
**先征询用户**：是否使用 grill-with-docs 方案来澄清？

- 用户同意 → 调用 `grill-with-docs` 技能执行澄清会话。
- 用户拒绝 → 按普通方式继续，不再重复询问。
- 简单一句话能说清的小改动（typo、单行修复）不必征询，直接做。

#### grill-with-docs 要点（已安装于 ~/.agents/skills/grill-with-docs/）

- **逐题拷问**：一次只问一个问题，等回答后再问下一个；每个问题附上推荐答案；能从代码里找到答案的问题自己查代码，不要问用户。
- **沿设计树推进**：按决策依赖关系逐分支走完整个设计树。
- **边聊边沉淀文档**：
  - 术语一经敲定，立即写入 `CONTEXT.md`（纯术语表，不含实现细节；采用「定义 + _Avoid_ 同义词」格式）。
  - 仅当决策同时满足「难逆转 / 脱离上下文会费解 / 真实权衡的结果」三条件时，才提议写 ADR 到 `docs/adr/NNNN-slug.md`（1-3 句话即可）。
  - 文件懒创建：没有 `CONTEXT.md` 就在敲定第一个术语时创建，没有 `docs/adr/` 就在需要第一份 ADR 时创建。
- **对抗性检查**：用词与既有术语表冲突、语义模糊、说法与代码实现矛盾时，当场指出并要求裁决。
